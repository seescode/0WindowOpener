'use strict';

module.exports = /*@ngInject*/
  function tabManagerService(/* inject dependencies here, i.e. : $rootScope */) {

    var openTab = function(url) {
      

    };

    return {
      // Do something awesome
      openTab: openTab
    };
  };
